# Optional: Razorpay Key ID for local Vite dev if the API proxy is not serving /api/payments/config.
# Prefer setting RAZORPAY_KEY_ID + RAZORPAY_KEY_SECRET in the project root .env for FastAPI instead.
# VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
